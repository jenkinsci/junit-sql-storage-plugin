unclassified:
  globalDatabaseConfiguration:
    database:
      postgreSQL:
        database: "postgres"
        hostname: "localhost"
        password: "postgres"
        username: "postgres"
        validationQuery: "SELECT 1"
  junitTestResultStorage:
    storage: "database"
